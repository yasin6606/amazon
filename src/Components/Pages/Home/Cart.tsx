import React from "react";
import {HomeCart} from "Assets/Types/GeneralTypes";

const Cart = ({item}: { item: HomeCart }): JSX.Element => {
    return <>
        <div key={item.key} className="basis-1/5 border bg-[#fff] shadow-md p-2">
            sss
        </div>
    </>;
};

export default Cart;